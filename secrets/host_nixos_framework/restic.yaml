# Placeholder secrets file for nixos-framework
# TODO: Replace these placeholder values and encrypt with SOPS using:
#   sops -e -i /etc/nixos/secrets/host_nixos_framework/restic.yaml
# You'll need to generate the age key for nixos-framework first and update .sops.yaml

remote_repo_uri: PLACEHOLDER_REPLACE_WITH_ACTUAL_RESTIC_REPO_URI
remote_repo_secret: PLACEHOLDER_REPLACE_WITH_ACTUAL_RESTIC_PASSWORD
runitor_ping_key: PLACEHOLDER_REPLACE_WITH_ACTUAL_HEALTHCHECK_KEY
